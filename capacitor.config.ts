import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.en2ovstesite.errandboy',
  appName: 'ErrandBoy',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
